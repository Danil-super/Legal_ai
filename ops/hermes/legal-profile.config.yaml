# Tool-free Hermes API-server baseline for Dental Legal AI.
# Provider/model values are intentionally not committed here; configure them in the
# deployed profile after reviewing the provider's data-processing terms.
#
# IMPORTANT: this file is accepted only together with ops/hermes/assert_tool_free.py.
# The preflight resolves Hermes' actual builtin/plugin registries and aborts startup
# if any model tool schema survives this configuration.

platform_toolsets:
  api_server: []

mcp_servers: {}

memory:
  memory_enabled: false
  user_profile_enabled: false

compression:
  enabled: false

checkpoints:
  enabled: false

smart_model_routing:
  enabled: false

session_reset:
  mode: none

tool_loop_guardrails:
  hard_stop_enabled: true
  hard_stop_after:
    exact_failure: 3
    idempotent_no_progress: 3
